"use client";

interface PromoBannerProps {
  className?: string;
}

/**
 * Retired component. The old "New Year Special" banner was removed in
 * favour of the inline "25% Off Your First Cleaning" sub-headline that
 * lives on the landing + zip pages. This stub stays so any old imports
 * keep compiling — it now renders nothing.
 */
export function PromoBanner(_props: PromoBannerProps) {
  return null;
}
