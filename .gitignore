# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.next
out
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
tsconfig.tsbuildinfo
supabase/.temp/

# Generated walkthrough recordings. The clips are published to the
# contractor-tour-recordings bucket; src/lib/tours/recordings.manifest.json is
# the committed record of what exists. Re-create with `npm run tours:record`.
docs/contractor-tours/recordings/*.webm
docs/contractor-tours/recordings/*.vtt

# Screenshots taken while verifying the contractor onboarding sequence.
# Evidence of a run, not a source of truth — re-create with
# `npm run onboarding:verify`.
docs/contractor-onboarding/*.png
