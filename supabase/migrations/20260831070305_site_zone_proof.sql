-- Applied on hosted as 20260831070305_site_zone_proof.
-- Local copy kept so schema_migrations versions line up. The hosted change
-- added zone-level photo proof columns already present in later walkthrough
-- migrations; this file is a no-op on a checkout that already has them.

SELECT 1;
