name: Delete test onboarding account

# One-time removal of the asannie74@gmail.com setup test.

on:
  push:
    branches: [cursor/applicant-phone-screen-email-600e]
    paths:
      - scripts/delete-test-account.py
      - .github/workflows/delete-test-account.yml

jobs:
  delete:
    runs-on: ubuntu-latest
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      PROJECT_REF: sxdraeptzuamsgjcvfeg
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Delete the test account
        run: python3 scripts/delete-test-account.py
